/**
 * Only controls the Library's category-chip ORDER — the 6 values here are
 * shown first, in this order. Any category string actually used by a
 * presets.ts entry or a community-components/*.json entry becomes a chip
 * automatically (see CATEGORIES in registry/index.ts), even if it isn't
 * listed here — this file doesn't need editing to add a new category.
 */
export const CATEGORIES_SEED: string[] = ['🏷️ markdown', '🧑‍💻 tech', '🌐 social', '📊 status', '✨ decoration', '💭 emotion'];
